MongoDB migrations with Docker
==============================

This is a Docker service for run MongoDB migrations with the [mongodb-migrations](https://www.npmjs.com/package/mongodb-migrations)
npm package.

